# CoFinder v1.9.x Development Log

This file records every development checkpoint after the published `v1.9.0` release. Keep it current so the next public release notes can accurately cover all `v1.9.x` work.

## v1.9.1

Status: planning branch.

User-visible runtime changes:

- None yet.

Planning changes:

- Bumped development version to `1.9.1`.
- Planned the v1.9.x hard-feature track:
  - multi-lane Jobs;
  - remote Copy To / Move To;
  - Search Contents / View Text;
  - expandable list rows;
  - icon, column, and gallery view modes.

Deferred:

- No runtime implementation in v1.9.1 planning.

Known risks:

- These features touch shared selection, Jobs, remote command execution, and pane rendering. Each checkpoint needs focused smoke testing before merge to `dev`.
